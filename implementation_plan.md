# WEBGAMES Portal Plan

## Goal

여러 웹게임을 모아 광고 수익을 내는 포털. `/` 홈에서 게임을 고르고 `/games/<id>/`에서 플레이한다 (Vite 멀티엔트리 MPA).

## Portal Work Packages

- [x] PT1 — GitHub 연동: origin 등록, 베이스라인 커밋/푸시, .gitignore
- [x] PT2 — 멀티엔트리 전환: 루트 홈 엔트리 + `games/forge/` 게임 엔트리, vite input 등록
- [x] PT3 — 홈 UI: 히어로, 게임 그리드, 광고 슬롯 placeholder, 정책 푸터(LegalDocsModal 재사용)
- [x] PT4 — 게임 레지스트리(`src/home/games.ts`)와 신규 게임 추가 절차 문서화(README)
- [x] PT5 — 검증: tsc+build exit 0, 데스크톱/모바일 실검, 콘솔 오류 0
- [ ] PT6 — 배포: 호스팅 선정(Vercel/Netlify 등), 도메인, og:image 절대 URL 교체
- [ ] PT7 — 광고: AdSense 신청, `.ad-slot` 실코드 교체, ads.txt
- [ ] PT8 — 두 번째 게임 기획·구현 (레지스트리 등록으로 홈 노출)

---

# Project Forge UI/UX & Economy Development Plan

## Goal

이미지 생성 자산과 Canvas 연출을 결합해 전투 피드백을 강화하고, 전투 → 골드 → 강화/매각 → 정수 성장의 경제 흐름을 한눈에 이해할 수 있는 모바일 UI로 개편한다.

## Fixed Decisions

- 기존 `project_forge_user_profile_v1` 저장 키와 핵심 게임 루프를 보존한다.
- 보스 보상은 한 런에서 각 마일스톤당 한 번만 지급한다. 매각 시에는 유지하고 런 정산 시 초기화한다.
- 검이 파괴된 동안 자동 공격, 탭 공격, 사냥 보상을 모두 중단한다.
- 강화 성공률과 균열 위험 표시에는 실제 판정과 동일한 계산을 사용한다.
- +20에서는 강화 버튼을 완료 상태로 대체한다.
- 생성 이미지는 프로젝트 내부 로컬 자산으로 저장하며 새 원격 의존성을 추가하지 않는다.
- 핵심 행동의 `alert`를 인앱 토스트 또는 결과 카드로 대체한다.

## Work Packages

- [x] P1 — 경제 계약: 계산 헬퍼, 저장 프로필 마이그레이션, 보스 보상 게이트, 파괴 상태 차단 기반
- [x] P2 — 생성 자산: 대장간 전투 배경과 적 캐릭터 로컬 WebP
- [x] P3 — 전투: 반응형 Canvas, 검광·충격·파편·피격·흔들림·보상 연출
- [x] P4 — 핵심 UI: 재화 HUD, 스테이지 보상, 검 비교, 강화/매각 CTA, 인앱 피드백
- [x] P5 — 검증: 타입 검사, production build, Playwright 상호작용, 390px/480px 시각 확인
- [x] P6 — 독립 Checker 재검수 (`PASS`)
- [x] P7 — 반응형 화면: 16:9 가로형 2열과 9:16 모바일 1열 자동 전환
- [x] P8 — 검 스프라이트: +0~+20 고유 외형, 5계열 재질 조합, 캔버스 공격 모션
- [x] P9 — 보스 사냥과 촉매 재료: +10~+19 전용 보스 10종, 반복 드롭·천장, 재료 충전식 강화 게이트
- [x] P10 — 생성 자산: 보스 10종 아틀라스와 전용 재료 10종 아이콘 아틀라스
- [x] P11 — 후반 전투/UI: 보스 추적 전환, 재료 획득 연출, 촉매 상태·도감·모바일 반응형
- [x] P12 — 통합 검증: 저장 마이그레이션, 드롭/천장/소모/초기화 경계, Playwright 및 독립 Checker
- [ ] P13 — 자동 조우 계약: +5 시작 셔플백, 저장된 보스 카드, 강제 조우와 새로고침 재추첨 차단
- [ ] P14 — 검 귀속 진행 촉매: 하급/상급 공용 충전, 확정 보상, 비축 상한과 매각·정산 수명주기
- [ ] P15 — 초월 수집: +18/+19 무기별 최초 판정, 실패 조각 보장, 계정 영구 유물
- [ ] P16 — 확률 계약 통합: 실제 판정·강화 패널·공식표가 같은 최종 4분포 사용
- [ ] P17 — 생성 자산과 UI: +5~+9 중간보스, 초월 유물, 보스 징조·보상·도감 반응형 화면
- [ ] P18 — R3 통합 검증: 마이그레이션/수명주기/경계 시뮬레이션, Playwright, 독립 Checker

## P9 Fixed Decisions

- +10→+11부터 +19→+20까지 현재 단계마다 전용 보스와 전용 촉매 재료가 하나씩 대응한다.
- 보스는 반복 사냥할 수 있다. 골드·정수 최초 처치 보상은 기존처럼 런당 한 번만 지급하지만 재료 드롭은 매 처치마다 독립 판정한다.
- 드롭률/천장은 +10~+19 순서로 `60/55/50/45/40/35/28/20/15/10%`, `3/3/4/4/5/5/6/8/10/15회`를 사용한다. 자연 드롭이 없으면 표기된 번째 처치에서 확정 지급하고, 재료를 획득하면 해당 보스의 천장 카운터를 0으로 초기화한다.
- 재료 한 개는 +10~+14에서 3회, +15~+17에서 2회, +18~+19에서 1회의 강화 충전을 제공한다. 해당 단계 강화 시 충전 1을 먼저 소모한다.
- +10 첫 재료는 시스템 학습을 위해 최초 발견 시 확정 지급한다. 이후에는 공개된 드롭률과 천장을 적용한다.
- 미장착 재료와 천장 진행도는 계정에 영구 보존한다. 검에 활성화된 충전은 성공 시 해당 단계에서 제거되고, 검 파괴·매각·런 정산 시 전부 제거한다.
- 기존 저장 키를 유지하고 신규 inventory/pity/charge 필드는 안전한 기본값으로 마이그레이션한다.
- 생성 이미지는 로컬 WebP 아틀라스로 저장하며 보스/재료 이름·확률·셀 위치는 하나의 데이터 계약에서 관리한다.

## P13-P18 Fixed Decisions

- 조우는 +5에서 열린다. 일반 적 처치마다 저장된 셔플백 카드를 한 장씩 공개하고, 보스 카드가 나오면 처치 전까지 같은 보스가 유지된다.
- 셔플백 크기는 `+5~9: 6`, `+10~13: 8`, `+14~17: 12`, `+18: 20`, `+19: 30`이다. 카드 위치는 최초 +5 도달 및 직전 보스 해결 직후에만 한 번 추첨하고 프로필에 저장한다. 단계 변화와 새로고침은 재추첨 사유가 아니다.
- 보스 외형은 조우가 공개된 순간의 강화 단계에 고정한다. +5~+9는 신규 중간보스 5종, +10~+19는 기존 단계별 보스 10종을 사용한다.
- 진행 촉매 드롭에는 RNG를 쓰지 않는다. `제련의 불씨`는 +10~+13 강화에, `심연의 인장`은 +14~+19 강화에 시도당 1회 소비한다.
- +5~+9 보스는 제련의 불씨를 최대 4회분까지 채운다. +10~+13 보스는 현재 막힘 방지를 위해 제련의 불씨를 3회분까지 채우고 심연의 인장을 최대 2회분 추가한다. +14~+17 보스는 심연의 인장을 2회분, +18~+19 보스는 1회분까지 채운다. 전체 보유 상한은 제련 4회, 심연 3회다.
- 현재 구간에서 실제 소비 가능한 진행 충전이 있으면 셔플백 진행을 멈춘다. 다른 구간용 충전, 희귀 조각, 레거시 재료는 정지 조건이 아니다. 충전이 0이 되면 기존 카드 위치에서 재개한다.
- 진행 촉매와 셔플백은 `weaponId`에 귀속한다. 성공·유지·균열·하락·파괴 대기·모의 광고 복구에서는 보존하고, 매각·런 정산·복구하지 않은 파괴 정산으로 새 검을 만들 때 초기화한다.
- +18 보스의 같은 무기 최초 적격 처치는 `3%`, +19는 `1%`로 완제품 초월 유물을 판정한다. 실패하면 해당 조각 1개를 확정 지급하며 40/100개에서 자동 합성한다. 희귀 판정은 같은 `weaponId + 단계`에서 한 번뿐이다.
- 같은 검의 첫 +20 강화 시도는 종말 조각 1개를, +20 성공은 종말 조각 2개를 추가 지급한다. 광고는 유물 확률·조각·진행 촉매에 영향을 주지 않는다.
- 최종 강화 분포는 성공/유지/균열/하락을 한 순수 계산에서 만든다. 균열 저항으로 감소한 실패 질량은 유지에 귀속하고, 실제 판정은 이 누적 분포에 단일 난수 한 번만 사용한다. 네 값의 내부 합계는 정확히 100이다.
- 기존 `catalystInventory`, `catalystPity`, `discoveredCatalysts`, `activeCatalystCharges`는 신규 진행 재료로 환전하지 않고 읽기 전용 레거시 아카이브로 보존한다.
- 실제 광고 SDK, 외부 서버 랭킹, 다중 탭 부정 방지와 서버 권위 판정은 이번 프로토타입 범위에서 제외한다.

## Definition of Done

- `npm run build`가 실제 TypeScript 및 Vite 빌드를 수행하고 성공한다.
- 일반 몬스터 처치, 보스 보상, 강화, 매각, 파괴, 복구, +20 경계 흐름이 계약대로 동작한다.
- 390×844 및 480×1000에서 잘림·중첩·가로 오버플로 없이 핵심 행동이 읽힌다.
- 전투 이펙트가 HP, 보상, CTA 정보를 가리지 않는다.
- 새 브라우저 콘솔 오류가 없다.
- 1280×720 및 1920×1080에서는 전투와 강화 패널이 좌우로 배치되고, 390×844 및 430×932에서는 기존 모바일 1열 흐름이 유지된다.
- +0부터 +20까지 강화 단계가 바뀔 때마다 검의 실루엣 또는 핵심 장식이 변하고, 5개 계열의 재질·룬·오라가 구분된다.
- 새 검 자산은 투명 매트와 고정 피벗을 사용하며, 자동 공격과 직접 공격의 예비·참격·충돌·복귀 모션이 캔버스에서 확인된다.
- +10 이상 강화는 현재 단계 전용 촉매 충전 없이는 실행되지 않고, UI가 필요한 보스·재료·보유량·천장 진행도를 정확히 안내한다.
- 보스 반복 처치에서 최초 보상은 중복되지 않으며 재료 드롭과 천장 초기화는 계약대로 동작한다.
- 보스 10종과 재료 10종이 서로 구분되는 생성 이미지로 전투 Canvas와 재료 UI에 표시된다.
- +5부터 수동 선택 없이 자동 셔플백 조우가 진행되고, 새로고침·단계 변화로 보스 위치나 활성 보스가 바뀌지 않는다.
- 진행 촉매는 보스 처치 시 확정 지급되며 충전 0 상태에서도 일반 사냥으로 유한 횟수 안에 다시 획득할 수 있다.
- 진행 촉매는 같은 검의 파괴 복구에는 남고 매각·정산으로 새 검을 만들면 사라지며, 기존 재료 데이터는 레거시 아카이브에 보존된다.
- +18/+19 희귀 판정은 같은 검·단계에서 한 번만 일어나고 실패 조각과 완제품은 계정에 영구 보존된다.
- 실제 강화 판정, 강화 패널, 공식 확률표의 성공/유지/균열/하락 합계가 동일하게 100%다.
