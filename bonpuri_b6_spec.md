# B6 스펙 — 보상 구조 개정 (추가형 → 교체형)

근거: `bonpuri_balance_review.md` + 시뮬레이터 실측(seeds 200, 42조합). 충돌 시 이 문서가 우선한다.

## 1. 목표

**보상에서 카드를 고르는 것이 손해인 구조를 없앤다.**

실측 결과 7개 덱 프리셋 **전부**에서 `건너뛰고 회복`이 `카드 선택`보다 클리어율이 높았다.

| 덱 | 카드 선택 | 회복 선택 |
| --- | ---: | ---: |
| 무구악용 | 0.5% | **73.0%** |
| 굿유틸 | 78.5% | 99.5% |
| 회복지연 | 88.0% | 100% |
| 문전좌정 | 90.0% | 100% |
| 기본덱 | 94.5% | 100% |

덱빌더에서 덱을 만들지 않는 것이 최적 전략인 상태다. 원인은 보상 카드가 덱을 **50 → 53 → 56 → 59 → 62장**으로 부풀려 기존 덱을 희석하기 때문이다.

## 2. 변경 파일 (목록 밖 수정 금지)

| 파일 | 성격 |
| --- | --- |
| `src/bonpuri/run/miniRun.ts` | 수정 — §3·§4 |
| `src/bonpuri/components/RewardScreen.tsx` | 수정 — 문구·교체 내역 표시 |
| `scripts/verify-bonpuri-contract.ts` | 수정 — 검사 갱신·추가 |

**절대 손대지 말 것**: `src/bonpuri/core/*`, `content/*`, `data/*`, `services/profile.ts`, `scripts/simulate-bonpuri.ts`(재측정 도구는 그대로 둬야 비교가 성립한다), `ref/`, `dadadak/`, `games/`, `vite.config.ts`, 그 외 `src/`·`scripts/`.

## 3. 카드 보상 — 교체형

보상 카드를 고르면:

1. 선택 카드 **3장**을 런 덱에 추가한다 (기존과 동일)
2. **동시에 덱에서 3장을 제거한다** → 덱 크기가 유지된다

### 3.1 제거 규칙 (결정적, 자동)

우선순위대로 3장을 제거한다.

1. **기본 카드를 우선 제거**하되 `신칼 : 넋가림 = 2 : 1` 로 뺀다 (5:4 비율 근사)
   - 즉 3장 제거 시 신칼 2 · 넋가림 1
2. 해당 기본 카드가 부족하면 남은 기본 카드(사설 풀기 포함)에서 채운다
3. 기본 카드가 전부 소진되면 **덱에서 수량이 가장 많은 카드**부터 제거한다. 동수면 카드 id 오름차순(결정적)
4. 어떤 경우에도 덱이 3장 미만이면 있는 만큼만 제거한다

> **제거 대상을 플레이어가 고르게 하지 않는다.** 이번 단계는 구조 효과만 측정한다. 선택 UI는 효과가 확인된 뒤 별도로 검토한다.

### 3.2 보관함

`collection` 에는 기존과 동일하게 **선택한 카드 1종만 +1** 한다. 제거된 기본 카드는 보관함에 영향을 주지 않는다(무제한 보유이므로).

## 4. 회복 보상 — 덱 압축을 겸하게

현재 `건너뛰고 명 10 회복` 이 지나치게 강하다. 다음으로 바꾼다.

```
건너뛰고 정화하기 — 명 8 회복 + 덱에서 기본 카드 1장 제거
```

- 회복량 **10 → 8**
- 제거는 §3.1의 우선순위를 따르되 **1장**만
- 제거할 기본 카드가 없으면 회복만 적용한다 (덱은 그대로)
- 명은 최대 70을 넘지 않는다

> 이렇게 하면 두 선택지가 **모두 덱을 개선**하되 방향이 다르다. 카드 선택은 파워, 정화는 압축+생존.

## 5. 화면 요구

- 보상 화면에 **무엇이 교체되는지 미리 보여준다**
  - 예: `자청비 3장이 들어오고, 신칼 2 · 넋가림 1이 빠집니다`
- 정화 선택지에도 표시: `명 8 회복 · 신칼 1장 제거`
- 덱 장수가 유지됨을 알 수 있게 현재 덱 크기를 표시한다
- 375px에서 가로 스크롤 금지

## 6. 제약

- **기존 계약 검사 1~52 중 보상 관련 항목만 갱신**하고 나머지는 그대로 통과해야 한다
- 엔진(`core/*`)은 건드리지 않는다 — 순수성 계약 유지
- `Math.random`·`Date.now` 금지, 주입 RNG만
- 새 의존성 금지

## 7. 수용 기준

```
npm run bonpuri:contract
npx tsc --noEmit
npm run build
npm run bonpuri:sim -- --seeds 200
```

앞의 셋은 exit 0. `bonpuri:sim` 은 실행되어 §8의 비교표를 낼 수 있어야 한다.

### 계약 검사

| # | 검사 |
| --- | --- |
| 49 | (갱신) 보상 선택 시 덱 크기가 **변하지 않고**, 선택 카드 3장이 들어가고 기본 카드 3장이 빠짐 |
| 53 | 제거 우선순위: 신칼 2 · 넋가림 1. 신칼이 부족하면 넋가림·사설 풀기로 채움 |
| 54 | 기본 카드가 0장인 덱에서는 수량 최다 카드부터 제거되고, 동수면 id 오름차순 (결정적) |
| 55 | 정화 선택 시 명 +8(최대 70), 기본 카드 1장 제거, 덱 크기 −1 |
| 56 | 정화 시 제거할 기본 카드가 없으면 회복만 적용되고 덱은 불변 |
| 57 | 4회 보상을 모두 카드로 받아도 런 종료 시 덱 크기가 시작과 동일 |

## 8. 완료 보고 (30줄 이내)

1. 변경 파일 목록
2. 수용 커맨드 4개 실행 결과
3. **개정 전후 시뮬레이터 비교표** — 최소한 아래 6행. 이게 이번 작업의 판정 근거다.

| 덱 | 정책 | 개정 전 카드선택 | 개정 후 카드선택 | 개정 후 정화 |
| --- | --- | ---: | ---: | ---: |
| 기본덱 | defensive | 94.5% | ? | ? |
| 무구악용 | defensive | 0.5% | ? | ? |
| 문전좌정 | defensive | 90.0% | ? | ? |
| 굿유틸 | defensive | 78.5% | ? | ? |
| 세경연계 | defensive | 99.5% | ? | ? |
| 차사처형 | defensive | 99.0% | ? | ? |

4. 카드별 사용 횟수 상위 15 (개정 전: 신칼 6368 · 넋가림 4311 · 사설 2664 · 4위 주목지신 90)
5. 기대값과 어긋난 항목 (수정하지 말고 그대로 보고)
6. 해석·가정한 부분
7. 미해결 항목

## 9. 금지사항

- `scripts/simulate-bonpuri.ts` 수정 — 비교 기준이 흔들린다
- `core/*`·`content/*`·`data/*`·`profile.ts` 수정
- 결과가 기대와 다르다고 §3·§4 규칙을 임의 조정 — 있는 그대로 측정하고 보고하라
- 검사 완화·삭제·skip
- 목록 밖 파일 수정
