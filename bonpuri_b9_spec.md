# B9 스펙 — 난이도 그리드 측정 (게임 로직 무변경)

근거: `bonpuri_system.md` §6.1 승천 스윕 결함 3건 + 확정된 난이도 목표. 충돌 시 이 문서가 우선한다.

## 1. 목표

난이도 재조정에 필요한 **측정값을 뽑는다.** 이번 단계는 **게임 로직을 한 줄도 바꾸지 않는다.**

확정된 난이도 목표:

| 지점 | 목표 클리어율 |
| --- | --- |
| 승천 0 · 기본덱 | **70~80%** |
| 최고 승천 · **최강덱** | **20~30%** |

두 가지를 모른다.

1. **적 수치를 얼마나 올려야 승천 0이 70~80%가 되는가** — 현재 100%
2. **최강덱이 무엇인가** — 지금 프리셋 7종은 설계자가 임의로 짠 것이라 진짜 상한을 모른다

## 2. 변경 파일 (목록 밖 수정 금지)

| 파일 | 성격 |
| --- | --- |
| `scripts/simulate-bonpuri.ts` | 수정 — 프리셋 추가 · `--grid` 모드 |

**절대 손대지 말 것**: `src/` **전체**, `ref/`, `dadadak/`, `games/`, `vite.config.ts`, `scripts/` 의 나머지 전부.

> 게임 로직을 안 건드리므로 계약 검사 63개는 자동으로 유지된다.

## 3. `최적화덱` 프리셋 추가

지금까지 측정에서 강했던 카드를 모은 **의도적 최강 후보** 덱을 프리셋에 추가한다. 이름은 `최적화덱`.

구성 (50장, 같은 카드 4장 상한 준수):

| 카드 | 장수 | 근거 |
| --- | ---: | --- |
| 제주 조왕신 (좌정) | 4 | 턴당 명 +3, 승천 배율에 가장 강함 |
| 제주 문전신 (좌정) | 4 | 턴당 넋 6 |
| 산판 (무구) | 4 | 매 턴 장단 +1 |
| 요령 (무구) | 4 | 매 턴 뽑기 +1 |
| 신맹두 (무구) | 4 | 주는 피해 +3 |
| 제주 삼승할망 | 4 | 명 12 회복 + 넋 8 (장단당 10.0) |
| 녹디생인 | 4 | 넋 8 (장단당 8.0) |
| 여산부인 | 4 | 명 8 + 넋 6 |
| 강림차사 | 4 | 처형 |
| 신칼 | 8 | 기본 공격 |
| 넋가림 | 4 | 기본 방어 |
| 사설 풀기 | 2 | 순환 |

> 이 구성은 **가설**이다. 진짜 최강덱이 아닐 수 있고, 측정 결과가 그걸 말해줄 것이다.

## 4. `--grid` 모드

적 피해 배율과 적 명 배율을 **독립적으로** 스윕한다. 승천 표와 무관하게 **순수 배율 그리드**를 잰다.

```
npm run bonpuri:sim -- --seeds 200 --grid
```

| 축 | 범위 |
| --- | --- |
| 적 `attack` 의도 피해 배율 | 1.0 / 1.1 / 1.2 / 1.3 / 1.4 / 1.5 / 1.6 |
| 적 명·최대 명 배율 | 1.0 / 1.1 / 1.2 / 1.3 / 1.4 |

- 덱은 **`기본덱` · `문전좌정` · `차사처형` · `최적화덱`** 4종
- 정책은 `defensive` **고정**, 보상은 `alwaysHeal`(정화) **고정**
- 배율 적용 규칙은 B8 §3.2와 동일: `attack` 의도의 `amount` 에만, `floor(값 × 배율)`, **원본 배열 불변**
- 시작 명 70, 정화 회복 8은 **고정**(이번 그리드에서는 배율 두 축만 변수)

### 4.1 출력

```
## 난이도 그리드 (seeds=200, defensive + 정화)
| 피해배율 | 명배율 | 기본덱 | 문전좌정 | 차사처형 | 최적화덱 |
```

각 셀은 **클리어율만** 적는다(잔여 명 등은 생략해 표를 읽을 수 있게).

### 4.2 요약 (필수)

표 아래에 두 줄을 낸다.

```
기본덱 70~80% 구간: (피해배율, 명배율) 조합 목록
최적화덱 20~30% 구간: (피해배율, 명배율) 조합 목록
```

해당 조합이 없으면 `없음(범위 밖)` 으로 적고, 가장 가까운 조합과 그 값을 함께 적는다.

## 5. 하위 호환

**`--grid` 없이 실행하면 출력이 지금과 완전히 동일해야 한다.** `--sweep` 도 그대로 동작해야 한다. `최적화덱` 프리셋 추가로 기존 42조합 표에 행이 늘어나는 것은 **허용**한다(덱이 8종이 되므로 48행). 단 기존 7종의 수치는 변하면 안 된다.

## 6. 수용 기준

```
npm run bonpuri:contract
npx tsc --noEmit
npm run bonpuri:sim -- --seeds 200
npm run bonpuri:sim -- --seeds 200 --grid
```

- `bonpuri:contract` 는 **63/63 그대로** 통과해야 한다 (게임 로직 무변경 확인)
- 기존 7개 덱의 승천 0 수치가 B8과 동일해야 한다
- `--grid` 가 §4.1 표와 §4.2 요약을 낸다

### 자가검증 (실행 시 출력)

| # | 검사 |
| --- | --- |
| 1 | `최적화덱` 이 정확히 50장이고 보상 카드가 4장을 넘지 않음 |
| 2 | 배율 1.0/1.0 셀의 결과가 `--grid` 없이 돌린 값과 동일 |
| 3 | 그리드 실행이 `content/enemies.ts` 원본을 변형하지 않음 |

## 7. 금지사항

- `src/` **전체** 수정 — 이번 단계는 측정 전용이다
- 결과가 마음에 안 든다고 프리셋·그리드 범위를 임의 조정
- `Math.random`·`Date.now` 사용 (시드 기반 RNG만)
- 새 의존성 추가

## 8. 완료 보고 (30줄 이내)

1. 변경 파일 목록
2. 수용 커맨드 4개 실행 결과
3. **§4.1 그리드 표 전문** — 이번 작업의 핵심 산출물
4. **§4.2 요약 두 줄**
5. `최적화덱` 이 기존 7종보다 실제로 강한지 (승천 0 기준 비교)
6. 기대값과 어긋난 항목
7. 해석·가정한 부분 / 미해결
