# B4 스펙 — 덱 50장 체제 전환

기획 근거: `bonpuri_b1_spec.md`, `bonpuri_b2_5_spec.md`, `bonpuri_b3_spec.md`. 충돌 시 이 문서가 우선한다.

## 1. 목표

시작 덱을 **10장 → 50장 고정**으로 바꾼다. 42종을 모아도 10장만 쓰던 구조라 수집이 장식이었다. 첫 손패를 7장으로 늘리고, 런 중 보상이 50장 덱에서도 체감되게 한다.

**적 수치는 이번에 건드리지 않는다** (§7 근거).

## 2. 변경 파일 (목록 밖 수정 금지)

| 파일 | 성격 |
| --- | --- |
| `src/bonpuri/core/battle.ts` | 수정 — 첫 턴 손패 7장 |
| `src/bonpuri/core/cards.ts` | 수정 — 기본 덱 50장 |
| `src/bonpuri/services/profile.ts` | 수정 — 덱 검증 50장·복사 상한 4 |
| `src/bonpuri/run/miniRun.ts` | 수정 — 보상 3장 투입 |
| `src/bonpuri/components/DeckEditor.tsx` | 수정 — 50장 편집 UX |
| `src/bonpuri/bonpuri.css` | 수정 |
| `src/bonpuri/App.tsx` | 수정 — 마이그레이션 안내 전달 (§6) |
| `scripts/verify-bonpuri-contract.ts` | 수정 — 검사 갱신·추가 |

> **스펙 정정(사후)**: 최초 작성 시 §6이 "본향에 안내"를 요구하면서 `App.tsx` 를 목록에서 빠뜨렸다. 구현자가 목록을 어기지 않고 미구현으로 보고한 것이 옳았고, 목록에 추가해 Claude가 직접 구현했다.

**절대 손대지 말 것**: `src/bonpuri/content/enemies.ts`(적 수치 동결), `src/bonpuri/content/cards.ts`(42종 동결), `src/bonpuri/data/*`, `src/bonpuri/core/rng.ts`, `ref/`, `dadadak/`, `games/`, `vite.config.ts`, `src/home/`, `src/waitdog/`, `src/services/`.

## 3. 덱 계약

| 항목 | 이전 | 이후 |
| --- | --- | --- |
| 시작 덱 크기 | 10장 고정 | **정확히 50장 고정** |
| 기본 덱 구성 | 신칼 5 · 넋가림 4 · 사설 풀기 1 | **신칼 25 · 넋가림 20 · 사설 풀기 5** |
| 같은 보상 카드 상한 | 2장 | **4장** |
| 기본 카드 3종 상한 | 무제한 | 무제한 (유지) |
| 첫 턴 손패 | 5장 | **7장** |
| 2턴 이후 손패 | 5장 | 5장 (유지) |
| 턴당 장단 | 3 | 3 (유지) |

- 덱이 50장이 아니면 `런 시작` 을 막고 사유를 표시한다
- 기본 덱 비율은 5:4:1 로 **이전과 동일**하다 (§7)

### 3.1 첫 턴 손패 계약

`startPlayerTurn` 은 `turn === 1` 일 때만 **7장**까지, 그 외에는 **5장**까지 채운다. `drawBonus` passive는 두 경우 모두에 더해진다.

> 주의: B1 §5.3의 "손패가 N장이 될 때까지 드로우" 의미(draw-to-N)를 유지한다. 턴 종료 시 손패를 전부 버리므로 손패 상한은 따로 두지 않는다.

## 4. 런 중 보상 계약

50장 덱에서 카드 1장은 2%라 체감되지 않는다. 다음으로 바꾼다.

- 보상으로 카드를 고르면:
  - **이번 런의 덱에 같은 카드 3장을 즉시 추가**한다 (버림더미로 들어간다 — 다음 셔플부터 등장)
  - **보관함(`collection`)에는 1종만 +1** 한다
- `건너뛰고 명 10 회복` 은 그대로
- 3장 추가는 덱 크기 제한(50장)의 영향을 받지 않는다. 런 중 덱은 50장을 넘을 수 있다

## 5. 덱 편집 UX

50장을 `+` 로 한 장씩 누르는 건 불가능하다.

- 카드마다 `−1` `+1` 에 더해 **`−5` `+5`** 버튼을 둔다
- 상단에 `n / 50장` 카운터를 상시 표시하고, 50이 아니면 눈에 띄게 표시한다
- `기본 덱으로 되돌리기` = 신칼 25 · 넋가림 20 · 사설 풀기 5
- **`남는 자리 기본 카드로 채우기`** 버튼을 추가한다 — 부족한 만큼 신칼·넋가림을 5:4 비율로 채워 50장을 맞춘다
- 보유 수량·상한을 넘는 증가는 버튼을 비활성화한다
- 375px에서 가로 스크롤이 생기면 안 된다

## 6. 저장 마이그레이션

기존 프로필의 `startingDeck` 은 10장이다. 그대로 로드하면 50장 검증에 걸린다.

- `schemaVersion` 을 **2** 로 올린다
- 로드 시 `schemaVersion === 1` 이면: `startingDeck` 을 **기본 덱 50장으로 교체**하고 `collection` 은 보존한다. 마이그레이션 후 즉시 저장한다
- 마이그레이션 사실을 본향에 1회 안내한다 (예: `덱 규격이 50장으로 바뀌어 기본 덱으로 되돌렸습니다`)
- 저장 실패 시 B3 §4.1의 fail-closed 규칙을 그대로 따른다

## 7. 적 수치를 건드리지 않는 근거 (구현자 참고)

덱이 5배가 되어도 초기 밸런스는 유지된다. 두 가지 이유다.

1. 기본 덱 비율이 5:4:1 로 동일하다 → 5장 손패의 기대 구성이 같다
2. 턴당 출력은 **장단 3** 이 묶는다. 덱 크기와 무관하다

따라서 `content/enemies.ts` 는 **동결**한다. 실제 플레이 후 재조정 여부를 판단한다.

## 8. 제약

- **기존 계약 검사는 갱신될 것만 갱신하고 나머지는 그대로 통과해야 한다.** 덱 크기·첫 손패 관련 검사(1·2번 등)는 새 값으로 갱신한다
- React 18 + TypeScript strict, 새 의존성 금지
- 엔진 순수성 유지 (검사 15)
- `Math.random`·`Date.now` 는 코어 밖에서만

## 9. 수용 기준

```
npm run bonpuri:contract
npx tsc --noEmit
npm run build
```

세 커맨드 모두 exit 0. 아래를 갱신·추가한다.

| # | 검사 |
| --- | --- |
| 1 | (갱신) 기본 덱이 신칼 25 · 넋가림 20 · 사설 풀기 5, 총 50장 |
| 2 | (갱신) 전투 시작 후 손패 **7**장, drawPile 43장, 장단 3, 명 70 |
| 46 | 2턴 이후 손패는 5장까지만 채워짐 |
| 47 | `drawBonus` 가 첫 턴(7+N)과 이후 턴(5+N) 모두에 적용 |
| 48 | 덱 검증: 50장이 아니면 거부, 보상 카드 5장째 거부, 기본 카드는 무제한 허용 |
| 49 | 보상 선택 시 이번 런 덱에 3장 추가되고 `collection` 은 1만 증가 |
| 50 | `schemaVersion: 1` 프로필 로드 시 덱이 기본 50장으로 교체되고 `collection` 은 보존 |
| 51 | 마이그레이션 결과가 즉시 저장되고, 저장 실패 시 fail-closed |

## 10. 금지사항

- `content/enemies.ts`·`content/cards.ts` 수정 (적 수치·42종 동결)
- 새 의존성 추가
- 이미지 파일 생성·참조
- 검사 완화·삭제·skip
- 목록 밖 파일 수정

## 11. 완료 보고 (30줄 이내)

1. 변경 파일 목록
2. 수용 커맨드 3개 실행 결과 (마지막 5줄씩)
3. 기대값과 어긋난 항목 (수정하지 말고 그대로 보고)
4. **직접 플레이 결과** — 50장 덱으로 1~2전투가 돌아가는지, 덱 편집이 50장에서 쓸 만한지
5. 해석·가정한 부분
6. 미해결 항목
