# B15-P 스펙 — 프레젠테이션 증명 패스

근거: WO-002(Phase A · 자산 생성) → **WO-003(Phase B · UI 배선)**. 충돌 시 최신 작업지시가 우선한다.

> **이 단계는 증명 실험이다.** 재미가 증명됐다고 선언하지 않는다. 판정 주체는 CEO이며, 실제 플레이 후에 내린다.
> 전체 배치 생성(나머지 카드 37종)·추가 적·추가 배경은 **별도 승인 대상**이다.

## 1. 목표

대표 자산 10점을 **실제 첫 전투에 배선**하고 최소한의 모션을 붙여, 시각 표현이 전투 이해와 손맛을 개선하는지 CEO가 **직접 판정 가능한 상태**로 만든다.

게임 규칙·수치는 바꾸지 않는다.

## 2. 자산 규격 (WO-003에서 확정 — 이전 3:4 / 16:9 규격은 폐기)

| 종류 | 해상도 | 비율 | 용도 |
| --- | --- | --- | --- |
| 카드 | **800×1200** | 2:3 세로 | 카드 전면 일러스트 |
| 적 | **800×1200** | 2:3 세로 | 초상화. 배경 위 자유 합성이 아니라 **어두운 초상 프레임** 안에 넣는다 |
| 배경 | **1350×900** | 3:2 가로 | 전투 화면 배경 |

### 2.1 안전 영역 규칙 폐기

기존 「상단 18% · 하단 22% 비움」 규칙은 **폐기한다.**

- 이미지는 **전면 일러스트**로 쓴다. 여백을 미리 비우지 않는다
- 가독성은 **UI가 책임진다** — 그림 위에 어두운 그라데이션을 깔고 그 위에 텍스트를 얹는다
- 구도가 프레임과 어긋나는 카드는 재생성하지 않고 **`object-position`만 개별 조정**한다

### 2.2 글자는 이미지에 넣지 않는다

카드명·비용·유형·효과·연계는 전부 **DOM 텍스트**다. 이미지에 글자를 그려 넣지 않는다.
이유: ① 수치가 바뀌면 이미지를 다시 뽑아야 한다 ② 스크린리더·번역이 불가능해진다 ③ 소형 화면에서 렌더 글자가 이미지 글자보다 항상 선명하다.

### 2.3 정규화 이력 (WO-003 1단계)

재생성 없이 중앙 크롭만 적용했다.

| 파일 | 전 | 후 | 조치 |
| --- | --- | --- | --- |
| `cards/munjeonsin.webp` | 849×1200 | 800×1200 | 좌 24 / 우 25 크롭. 잘린 구간 최대 밝기 57/255(순수 비네트), 구조물은 x=42..790으로 크롭 창 안에 전부 포함 |
| `cards/jacheongbi.webp` | 801×1200 | 800×1200 | 우 1px 크롭 |

나머지 8점은 이미 규격에 맞아 손대지 않았다.

## 3. 화풍 계약 (WO-002에서 확정 · 유지)

- **독자 화풍**: K-웹툰·게임 일러스트 + 오방색(청·적·황·백·흑). 특정 작가나 기존 IP(포켓몬·유희왕 등)의 구도·디자인을 모사하지 않는다
- **신별 톤 분리**: 선신은 따뜻하게(금빛) / 명부·저승은 서늘하게(보랏빛). 선·악·중립이 화면에서 읽혀야 한다
- **문화적 취급**: 신격과 의례를 희화화하지 않는다. 포획·수집 대상으로 표현하지 않는다
- **소형 가독성**: 손패 카드 폭 130px까지 줄여도 주체와 유형이 구분되는 단순 실루엣·명확한 주색

## 4. 대표 자산 10점

| # | 파일 | 대상 | 카드 ID | 유형 | 톤 의도 |
| --- | --- | --- | --- | --- | --- |
| 1 | `cards/sinkal.webp` | 신칼 | `sinkal` | 신(기본 공격) | 중립·날카로움. 백색 섬광 |
| 2 | `cards/neokgarim.webp` | 넋가림 | `neokgarim` | 신(기본 방어) | 중립·수호. 청색 결계 |
| 3 | `cards/saseol.webp` | 사설 풀기 | `saseol` | 신(기본 순환) | 중립·지혜. 금빛 두루마리 |
| 4 | `cards/jacheongbi.webp` | 자청비 | `jacheongbi` | 신(세경본풀이) | **선신·따뜻함**. 황금 곡식 |
| 5 | `cards/gangnimchasa.webp` | 강림차사 | `gangnimchasa` | 신(저승차사) | **명부·서늘함**. 보랏빛 사슬 |
| 6 | `cards/myeongdu.webp` | 명두 | `myeongdu` | 무구 | 신비·거울. 은청색 |
| 7 | `cards/yeongdeunggut.webp` | 영등굿 | `yeongdeunggut` | 굿(의례) | 역동·바람. 청록 |
| 8 | `cards/munjeonsin.webp` | 제주 문전신 | `munjeonsin` | 좌정(가택) | 수호·정적. 황동 문 자체가 주역(인물 없음) |
| 9 | `enemies/japgwi.webp` | 잡귀 | 적 `japgwi` | — | 위협적이나 코믹. 탁한 녹회색 |
| 10 | `backgrounds/isung.webp` | 이승 | 1전투 배경 | — | 인물이 앞에 설 자리를 비운 무대 |

**총 1.07MB.**

### 4.1 ID 매칭 규칙

런타임 카드 ID는 `기본ID#일련번호` 형태다(`sinkal#7`, `jacheongbi#0`). 일러스트는 **`#` 앞의 기본 ID로만 찾는다.**
매핑이 없는 카드(나머지 37종)는 **깨진 이미지 없이 기존 텍스트 카드로 표시한다.** 이것이 정상 동작이지 결함이 아니다.

## 5. UI 배선 계약 (WO-003 2단계)

변경 허용 파일은 다음뿐이다. 목록 밖 수정은 금지한다.

```
src/bonpuri/content/presentation.ts   (신규 — 자산 매핑 단일 지점)
src/bonpuri/App.tsx
src/bonpuri/components/BattleScreen.tsx
src/bonpuri/components/CardView.tsx
src/bonpuri/components/RewardScreen.tsx
src/bonpuri/components/EndScreen.tsx
src/bonpuri/components/RulesPanel.tsx
src/bonpuri/bonpuri.css
```

`core/`, `run/`, `services/`, `content/cards.ts`, `content/enemies.ts`, `content/ascension.ts`는 **손대지 않는다.**

### 5.1 표시 계약

1. 대표 8종만 그림을 표시한다. 나머지는 기존 텍스트 카드
2. 카드는 전면 그림 위에 어두운 그라데이션 → 그 위에 비용·이름·유형·효과를 DOM 텍스트로
3. 첫 전투에 `isung` 배경과 `japgwi` 초상을 표시한다
4. 잡귀는 배경 위 자유 합성이 아니라 **어두운 테두리의 초상 프레임** 안에
5. 플레이어·적의 명을 **실제 비율의 바**로 표시하고 값 변화에 애니메이션

### 5.2 모션 계약

| # | 연출 | 조건 |
| --- | --- | --- |
| 1 | 카드 사용 시 짧은 상승·발광 | |
| 2 | 적 명 감소 시 **감소량 숫자 + 피격 흔들림** | **정확히 한 번만.** 실제 감소가 있을 때만 |
| 3 | 승리 → 보상 화면 전환·카드 등장 | |

**불변 조건:**

- 애니메이션이 게임 계산을 **지연시키지 않는다.** 상태는 즉시 확정되고 연출은 그 위에 얹힌다
- 같은 카드를 **두 번 사용하게 만들지 않는다**
- `prefers-reduced-motion`에서 모든 핵심 모션을 제거하거나 즉시 완료한다. 조작과 상태 전달은 유지된다
- 넋가림·사설 풀기처럼 피해가 없는 카드에서 **피해 숫자가 뜨지 않는다**
- 새 의존성을 추가하지 않는다

## 6. 완료 기준 (WO-003)

1. 첫 전투에서 기본 카드 3종의 그림이 모두 보임
2. 이승 배경과 잡귀 초상이 의도한 프레임으로 보임
3. 신칼 사용 시 적 명이 **정확히 한 번** 감소
4. 감소 숫자·명 바·피격 연출이 실제 상태와 일치
5. 넋가림·사설 풀기에서 잘못된 피해 숫자가 표시되지 않음
6. 그림 없는 보상 카드도 기존 형태로 정상 표시
7. **375×812**와 **1440×1200**에서 글자가 읽히고 가로 넘침 없음
8. 모바일 규칙 패널이 손패와 턴 종료 버튼을 가리지 않음
9. reduced-motion 환경에서 조작과 상태 전달 유지
10. 브라우저 콘솔·페이지 오류 0건
11. `npm run bonpuri:contract` **64/64** · `npm run build` 통과 (Claude가 직접 실행)
12. 데스크톱·모바일 전투/보상 스크린샷 제출

## 7. 금지 (WO-003)

- 나머지 카드 37종 생성 · 추가 적 · 추가 배경 생성
- `core/` `run/` `services/` · 카드·적 데이터 수정
- 카드 비용·효과·적 수치·보상 로직 변경
- 사운드 추가
- GPS·QR·AR·계정·PvP 작업
- 패키지 설치
- 전체 작업 자동 스테이징
- merge · rebase · fetch · pull · push
- 재미가 증명됐다는 자체 선언
