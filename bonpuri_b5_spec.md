# B5 스펙 — 밸런스 시뮬레이터 (측정 전용)

기획 근거: `bonpuri_balance_review.md`, 외부 검토 3건. 충돌 시 이 문서가 우선한다.

## 1. 목표

현재 밸런스의 **기준선을 우리 엔진으로 직접 측정**한다. 외부 검토 3건이 클리어율을 두고 "거의 못 깬다" vs "사실상 100%"로 정반대 결론을 냈다. 규칙 해석 차이일 가능성이 크므로 실제 엔진으로 재봐야 한다.

**게임 코드는 한 줄도 바꾸지 않는다.** 순수 측정 도구다.

## 2. 변경 파일 (목록 밖 수정 금지)

| 파일 | 성격 |
| --- | --- |
| `scripts/simulate-bonpuri.ts` | 신규 |
| `package.json` | `scripts` 에 `bonpuri:sim` 1줄 추가만 |

**절대 손대지 말 것**: `src/` 전체, `ref/`, `dadadak/`, `games/`, `vite.config.ts`, 기존 `scripts/*`.

## 3. 최상위 제약 — 엔진 재구현 금지

시뮬레이터는 반드시 기존 모듈을 **import 해서 그대로** 쓴다.

```ts
import { startBattle, playCard, endTurn } from '../src/bonpuri/core/battle';
import { createStartingDeck } from '../src/bonpuri/core/cards';
import { miniRunEnemies } from '../src/bonpuri/content/enemies';
import { rewardCards, cloneRewardCard } from '../src/bonpuri/content/cards';
import { advanceRun, chooseReward, skipReward, ... } from '../src/bonpuri/run/miniRun';
```

- 전투 규칙·피해 공식·상태이상·보상 로직을 **다시 짜지 마라.** 규칙을 복제하면 측정값이 실제 게임과 달라진다
- 엔진 함수는 순수하므로 부작용 없이 반복 호출 가능하다

### 3.1 `miniRun.ts` 가 런 전체를 구동할 API를 이미 노출한다

```ts
startMiniRun(rng, startingDeck?)        → MiniRunState
playRunCard(state, handIndex, rng)      → MiniRunState
endRunTurn(state, rng)                  → MiniRunState
chooseReward(state, rewardIndex, rng)   → MiniRunState
skipReward(state, rng)                  → MiniRunState
// state.phase: 'battle' | 'reward' | 'won' | 'lost'
```

**시뮬레이터는 이 5개 함수만으로 런을 끝까지 돌릴 수 있다.** 전투 루프를 직접 조립하지 마라 — `state.phase` 를 보고 분기하면 된다.

`startMiniRun` 의 `startingDeck` 은 카드 id 배열(50개)이다. §5의 프리셋을 이 형식으로 만든다.

## 4. 실행

```
npm run bonpuri:sim                  # 기본: 시드 500
npm run bonpuri:sim -- --seeds 2000  # 표본 수 지정
npm run bonpuri:sim -- --deck 기본덱  # 특정 덱만
```

- RNG는 **시드 기반 결정적 생성기**를 쓴다. `Math.random`·`Date.now` 금지
- 같은 시드 → 같은 결과 (§8 검사 2번)

## 5. 덱 프리셋 7종 (각 정확히 50장)

기본 카드로 나머지를 채워 50장을 맞춘다(신칼:넋가림 = 5:4 비율, 사설 풀기는 5장 고정).

| 이름 | 구성 | 측정 의도 |
| --- | --- | --- |
| `기본덱` | 신칼 25 · 넋가림 20 · 사설 5 | 기준선 |
| `무구악용` | 산판 4 · 요령 4 · 신맹두 4 · 명두 4 · 물색 4 + 기본 30 | **무구 무제한이 얼마나 센지 정량화** |
| `회복지연` | 조왕신 4 · 심방쾌자 4 · 터주신 4 · 제주 삼승할망 4 + 기본 34 | **지연 악용이 성립하는지** |
| `세경연계` | 자청비 4 · 문도령 4 · 정수남 4 + 기본 38 | 소형 계열(3종) 연계 발동률 |
| `문전좌정` | 제주 문전신 4 · 녹디생인 4 · 제주 조왕신 4 · 남선비 4 · 여산부인 4 + 기본 30 | 대형 계열(8종) 연계 |
| `차사처형` | 강림차사 4 · 일직차사 4 · 월직차사 4 · 저승사자 4 + 기본 34 | 처형 카드 실효성 |
| `굿유틸` | 영등굿 4 · 시왕맞이 4 · 성주풀이 4 · 초감제 4 + 기본 34 | OP 의심 굿 카드 검증 |

## 6. 플레이 정책

### 6.1 카드 사용 정책 3종

| 이름 | 규칙 |
| --- | --- |
| `defensive` | ① 처형 가능하면 처형 ② 적 예상 피해를 막을 만큼 넋 확보 ③ 남은 장단으로 피해 최대화 |
| `aggressive` | 방어를 하지 않고 피해 최대화만 |
| `random` | 낼 수 있는 카드 중 무작위 (하한선) |

공통 규칙:
- 대상은 항상 **살아있는 첫 적**
- 비용 0 카드(사설 풀기)는 항상 먼저 사용
- 무구·좌정은 **낼 수 있으면 즉시** 사용
- 굿은 턴당 1장 제한을 지킨다
- 더 이상 낼 카드가 없으면 턴 종료

> `defensive` 가 외부 검토3이 "클리어율 100%"를 낸 정책이다. 이 정책의 결과가 그 주장을 검증한다.

### 6.2 보상 선택 정책 2종

| 이름 | 규칙 |
| --- | --- |
| `synergy` | 3장 중 이미 덱에 있는 계열 우선, 없으면 첫 번째 |
| `alwaysHeal` | 항상 `건너뛰고 명 10 회복` |

> 이 둘의 비교가 검토들이 충돌한 **"카드 3장 vs 명 10"** 교환비에 답한다.

### 6.3 무한 루프 방지

- 한 전투가 **50턴**을 넘으면 그 런을 `무승부(stalled)` 로 기록하고 중단한다
- `stalled` 비율은 별도로 보고한다 — **지연 악용의 직접 지표**다

## 7. 출력

stdout에 표 형태로 출력한다. 파일을 쓰지 않는다(필요하면 사용자가 리다이렉트).

### 7.1 조합별 요약 (덱 7 × 사용정책 3 × 보상정책 2 = 42행)

| 열 | 내용 |
| --- | --- |
| 덱 / 사용정책 / 보상정책 | 조합 식별 |
| 클리어율 | 5전투 완주 비율 (%) |
| 잔여 명 중앙값 | 클리어한 런의 최종 명 |
| 패배 지점 | 1~5전투별 패배 비율 |
| 평균 턴 | 전투당 평균 턴 수 |
| stalled | 50턴 초과 비율 |

### 7.2 추가 집계 (기본 덱 + `defensive` + `synergy` 기준)

- **연계 발동 횟수** — 계열별로, 스택 1 이상으로 낸 카드 수
- **카드별 사용 횟수 상위 15** — 어떤 카드가 실제로 쓰이는지
- **무구 평균 장착 수** — `무구악용` 덱에서 실제로 몇 개가 깔리는지

## 8. 수용 기준

```
npm run bonpuri:sim -- --seeds 200
npm run bonpuri:contract
npx tsc --noEmit
```

- `bonpuri:sim` 이 exit 0 으로 끝나고 §7의 표가 전부 출력된다
- **`bonpuri:contract` 가 기존 52/52 그대로 통과한다** (게임 코드 무변경 확인)
- `npx tsc --noEmit` 이 exit 0

추가 검사 (스크립트 자체 자가 검증, 실행 시 출력):

| # | 검사 |
| --- | --- |
| 1 | 모든 덱 프리셋이 정확히 50장이고 같은 보상 카드가 4장을 넘지 않음 |
| 2 | 같은 시드·같은 조합을 2회 실행하면 결과가 완전히 동일 |
| 3 | 엔진 호출 중 `Math.random` 미사용 (RNG 주입만) |

셋 중 하나라도 실패하면 non-zero 종료.

## 9. 금지사항

- `src/` 하위 **어떤 파일도** 수정
- 전투 규칙·피해 공식·상태이상·보상 로직의 **재구현**
- `Math.random`·`Date.now` 사용
- 새 의존성 추가
- 파일 쓰기 (stdout만)
- 결과가 마음에 안 든다고 정책·프리셋을 임의 조정 — 있는 그대로 측정하고 보고하라

## 10. 완료 보고 (40줄 이내)

1. 변경 파일 목록
2. 수용 커맨드 3개 실행 결과
3. **§7.1 요약표 전문** (42행) — 이게 이번 작업의 핵심 산출물이다
4. §7.2 추가 집계
5. 측정에서 눈에 띈 것 (해석은 짧게, 수치를 우선)
6. 해석·가정한 부분 (특히 정책 구현에서 판단이 갈린 지점)
7. 미해결 항목
