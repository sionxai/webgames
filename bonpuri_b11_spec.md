# B11 스펙 — 연계 스택 분포 측정 (게임 로직 무변경)

근거: `bonpuri_system.md` §6.4. 충돌 시 이 문서가 우선한다.

## 1. 목표

연계(緣) 스케일링을 묶는 처방 — **스택 상한 / 턴 단위 리셋 / perStack 하향** — 중 무엇이 유효한지는 **카드를 낼 때 스택이 실제로 몇이었는가의 분포**가 결정한다. 그걸 잰다.

동시에 §6.4의 슈퍼선형 가설(적 명이 클수록 전투가 길어져 스택이 더 쌓인다)을 난이도 3지점에서 검증한다.

**이번 단계는 게임 로직을 한 줄도 바꾸지 않는다.**

## 2. 변경 파일 (목록 밖 수정 금지)

| 파일 | 성격 |
| --- | --- |
| `scripts/simulate-bonpuri.ts` | 수정 — `--bonds` 모드 추가 |

**절대 손대지 말 것**: `src/` **전체**, `ref/`, `dadadak/`, `games/`, `vite.config.ts`, `scripts/` 의 나머지 전부.

## 3. 측정 방법

시뮬레이터는 이미 `MiniRunState.battle.playedMyths` 를 볼 수 있다. **`playRunCard` 를 호출하기 직전에** 낼 카드의 `bondGroup` 스택을 읽으면 된다. 엔진 수정이 필요 없다.

```
스택 = state.battle.playedMyths[card.bondGroup] ?? 0   (카드를 내기 직전 값)
보너스 = card.bond.perStack × 스택                      (bond 있는 카드만)
```

- `bond` 필드가 있는 카드만 집계한다 (`bondGroup` 만 있고 `bond` 없는 카드는 스택만 올리고 보너스가 없다 — 집계에서 제외)
- 보너스 합산은 `applyTo` 구분 없이 수치 합으로 잰다 (damage·block 나눠서도 병기)

## 4. `--bonds` 모드

```
npm run bonpuri:sim -- --seeds 200 --bonds
```

- 덱: **차사처형 · 문전좌정 · 세경연계** 3종 (연계 밀도 상위)
- 정책: `defensive` + `alwaysHeal` 고정
- 난이도 3지점: **(1.0/1.0) · (1.3/1.3) · (1.6/1.4)** — 배율 적용 규칙은 B9 `--grid` 와 동일 (attack만, floor, 원본 불변)

### 4.1 출력 ① — 스택 히스토그램 (덱 × 난이도별)

bond 카드를 낸 순간의 스택 분포. **처방 선택의 직접 근거다.**

```
| 덱 | 난이도 | 0스택 | 1 | 2 | 3 | 4 | 5 | 6+ | 낸 bond 카드 수 |
```

각 셀은 비율(%)로 적는다.

### 4.2 출력 ② — 스택 요약과 보너스 기여

```
| 덱 | 난이도 | 전투당 최대 스택(중앙값) | 평균 전투 턴 | bond 보너스 총량(런 평균) | 총 피해 중 보너스 비중 |
```

- `총 피해 중 보너스 비중` = damage 보너스 합 ÷ (기본 damage 합 + damage 보너스 합). 분모는 bond 카드의 기본 `amount` 합으로 근사한다 (신칼 등 무연계 카드 피해는 제외해도 된다 — 근사임을 출력에 명시)

### 4.3 출력 ③ — 판정 보조 두 줄 (필수)

```
스택 4 이상에서 낸 bond 카드 비율: 차사처형 X% / 문전좌정 Y% / 세경연계 Z%   (1.0/1.0 기준)
난이도 상승 시 최대 스택 변화: 차사처형 A→B, 문전좌정 C→D, 세경연계 E→F   (1.0/1.0 → 1.6/1.4)
```

> 해석 기준(참고): 스택 4+ 비율이 유의미하면(≳15%) **상한 3**이 효과가 있다. 거의 0이면 상한은 헛다리고 perStack 하향·턴 리셋 쪽이다. 난이도 상승 시 최대 스택이 커지면 슈퍼선형 가설이 확정된다.

## 5. 하위 호환

`--bonds` 없이 실행하면 출력이 지금과 **완전히 동일**해야 한다. `--grid`·`--sweep` 도 그대로 동작해야 한다.

## 6. 수용 기준

```
npm run bonpuri:contract
npx tsc --noEmit
npm run bonpuri:sim -- --seeds 200
npm run bonpuri:sim -- --seeds 200 --bonds
```

- `bonpuri:contract` 는 **63/63 그대로** (게임 로직 무변경 확인)
- 셋째 커맨드의 기존 표 수치가 B10과 동일
- 넷째가 §4.1~4.3 을 전부 출력

### 자가검증 (실행 시 출력)

| # | 검사 |
| --- | --- |
| 1 | 같은 시드 2회 실행 결과 동일 (결정성) |
| 2 | 히스토그램 각 행의 비율 합이 100% (반올림 오차 ±0.5%p 허용) |
| 3 | 측정이 `content/enemies.ts`·`content/cards.ts` 원본을 변형하지 않음 |

## 7. 금지사항

- `src/` 전체 수정 — 측정 전용
- 결과에 따라 덱·정책·난이도 지점을 임의 조정
- `Math.random`·`Date.now` 사용
- 새 의존성 추가

## 8. 완료 보고 (30줄 이내)

1. 변경 파일 목록
2. 수용 커맨드 4개 실행 결과
3. **§4.1 히스토그램 전문**
4. **§4.2 요약표 전문**
5. **§4.3 판정 보조 두 줄**
6. 기대값과 어긋난 항목 (수정하지 말고 그대로 보고)
7. 해석·가정한 부분 / 미해결
