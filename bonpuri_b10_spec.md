# B10 스펙 — 처형(execute) 절대값 전환 + 재측정

근거: `bonpuri_system.md` §6.2 B9 발견 2·3. 충돌 시 이 문서가 우선한다.

## 1. 목표

**처형이 난이도 배율에 반응하게 만든다.**

B9 측정에서 차사처형 덱은 기본덱이 **0%** 가 되는 배율(피해 1.6 / 명 1.4)에서도 **94.5%** 였다. 원인은 처형이 **비율 기준**이라는 것이다.

```
현재: 적 명 70 → 30% = 21 이하 처형
      적 명 98 (×1.4) → 30% = 29 이하 처형   ← 임계 절대량이 함께 커진다
```

적 명을 올릴수록 처형이 **더 강해진다.** 배율 방식 난이도가 구조적으로 안 통한다.

**절대값 기준으로 바꾸면 반대가 된다.**

```
변경: 적 명 70 → 16 이하 처형 (23%)
      적 명 98 → 16 이하 처형 (16%)          ← 상대적으로 약해진다
```

## 2. 변경 파일 (목록 밖 수정 금지)

| 파일 | 성격 |
| --- | --- |
| `src/bonpuri/core/types.ts` | 수정 — `Effect` 의 execute 분기 |
| `src/bonpuri/core/battle.ts` | 수정 — execute 판정 1곳 |
| `src/bonpuri/content/cards.ts` | 수정 — 강림차사·저승사자 2장 |
| `src/bonpuri/components/CardView.tsx` | 수정 — 카드 문구 |
| `scripts/verify-bonpuri-contract.ts` | 수정 — 검사 갱신 |

**절대 손대지 말 것**: `core/cards.ts`·`core/enemies.ts`·`core/rng.ts`, `content/enemies.ts`, `content/ascension.ts`, `data/*`, `run/*`, `services/*`, 그 외 `components/*`, `scripts/simulate-bonpuri.ts`(비교 기준), `ref/`, `dadadak/`, `games/`, `vite.config.ts`.

> **엔진을 건드리는 첫 단계다.** 계약 검사 63개 중 execute 관련(21·21-1)과 카드 표(23·25)만 갱신 대상이고, **나머지 59개는 그대로 통과해야 한다.** 특히 순수성(15)·결정성(14·62)·입력 불변성은 절대 깨지면 안 된다.

## 3. 타입 변경

```ts
// 변경 전
| { kind: 'execute'; threshold: number; amount: number; target: 'enemy' }
// 변경 후
| { kind: 'execute'; thresholdHp: number; amount: number; target: 'enemy' }
```

**필드 이름을 바꾼다.** `threshold`(비율)를 남겨두고 의미만 바꾸면 나중에 읽는 사람이 반드시 헷갈린다.

### 3.1 판정 규칙

```
대상의 hp <= thresholdHp  →  hp = 0 (즉사, 넋 무시, 피해 공식 미적용)
그 외                      →  amount 를 §2.3 피해 공식으로 적용
```

- `maxHp` 를 더 이상 참조하지 않는다
- 나머지 동작(단일 대상 · 유효 대상 없으면 완전 거부 · 연계 보너스는 `amount` 에만 적용되고 `thresholdHp` 는 불변)은 **기존 그대로 유지**한다

## 4. 카드 수치

| 카드 | 비용 | 변경 전 | 변경 후 |
| --- | ---: | --- | --- |
| 강림차사 | 3 | 명 **30%** 이하 처형 / 실패 시 21 | 명 **16 이하** 처형 / 실패 시 21 |
| 저승사자 | 2 | 명 **25%** 이하 처형 / 실패 시 12 | 명 **10 이하** 처형 / 실패 시 12 |

참고 — 적별 처형 구간 변화 (배율 1.0 기준):

| 적 | 명 | 강림차사 전 | 강림차사 후 |
| --- | ---: | ---: | ---: |
| 잡귀 | 22 | 6 (27%) | 16 (73%) |
| 굴뱀 | 45 | 13 (29%) | 16 (36%) |
| 구삼승할망 | 70 | 21 (30%) | **16 (23%)** |
| 구삼승할망 ×1.4 | 98 | 29 (30%) | **16 (16%)** |

앞 전투에서는 강해지고 **보스와 고배율에서 약해진다.** 의도한 방향이다.

## 5. 화면 문구

`CardView` 의 execute 표시를 `명 16 이하 처형 · 실패 시 피해 21` 형태로 바꾼다. 퍼센트 표기를 남기지 마라.

## 6. 수용 기준

```
npm run bonpuri:contract
npx tsc --noEmit
npm run build
npm run bonpuri:sim -- --seeds 200 --grid
```

앞의 셋은 exit 0. **계약 검사는 63개를 유지**한다(항목 수는 그대로, 내용만 갱신).

### 갱신할 검사

| # | 갱신 내용 |
| --- | --- |
| 21 | `thresholdHp` 이하면 즉사(넋 무시), 초과면 `amount` 가 피해 공식대로 적용 |
| 21-1 | execute 카드의 연계 보너스가 `amount` 에만 적용되고 **`thresholdHp` 는 변하지 않음** |
| 23 · 25 | 카드 표 대조를 §4 값으로 |

**나머지 검사는 손대지 마라.** 하나라도 실패하면 구현이 잘못된 것이다.

## 7. 완료 보고 (30줄 이내)

1. 변경 파일 목록
2. 수용 커맨드 4개 실행 결과
3. **그리드 비교표** — B9 대비 `차사처형` 열이 어떻게 변했는지. 아래 6행은 필수

| 피해 / 명 | B9 차사처형 | **B10 차사처형** | B10 기본덱 | B10 문전좌정 | B10 최적화덱 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1.0 / 1.0 | 100% | ? | ? | ? | ? |
| 1.3 / 1.3 | 100% | ? | ? | ? | ? |
| 1.4 / 1.2 | 100% | ? | ? | ? | ? |
| 1.5 / 1.0 | 100% | ? | ? | ? | ? |
| 1.6 / 1.3 | 98.5% | ? | ? | ? | ? |
| 1.6 / 1.4 | 94.5% | ? | ? | ? | ? |

4. **처형이 배율에 반응하는가** — 1.0/1.0 대비 1.6/1.4에서 차사처형이 얼마나 떨어졌는지 한 줄로
5. 기대값과 어긋난 항목 (수정하지 말고 그대로 보고)
6. 해석·가정한 부분 / 미해결

## 8. 금지사항

- §2 목록 밖 파일 수정 — 특히 `simulate-bonpuri.ts`(비교 기준)와 `content/enemies.ts`(적 수치)
- `threshold` 필드를 남겨둔 채 의미만 변경
- execute 외의 다른 카드·효과 수치 변경
- §4 값의 임의 조정 — **그리드 결과가 목표에 못 미쳐도 표대로 구현하고 그대로 보고하라.** 재조정은 내가 판단한다
- 검사 완화·삭제·skip
