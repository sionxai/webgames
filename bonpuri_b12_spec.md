# B12 스펙 — 연계 스택 상한 3 (본풀이 완성)

근거: `bonpuri_system.md` §6.5 (B11 스택 분포 측정). 충돌 시 이 문서가 우선한다.

## 1. 목표

연계 보너스 배수에 **상한 3**을 건다.

B11 측정 근거: 문전좌정 덱은 bond 카드 플레이의 **56.4%가 스택 4 이상**에서 나오고 가치의 71~75%가 보너스다(폭주). 차사처형·세경연계는 4+ 비율이 2.7%/5.6%라 **상한 3의 영향을 거의 받지 않는다.** 폭주 덱만 정확히 겨냥하는 처방이다.

서사: **연 3 = 본풀이 완성.** 같은 본풀이를 세 번 읊으면 이야기가 완성되어 신령이 온전히 강림한다 — 그 이상 반복해도 더 강해지지 않는다.

## 2. 변경 파일 (목록 밖 수정 금지)

| 파일 | 성격 |
| --- | --- |
| `src/bonpuri/core/battle.ts` | 수정 — 289행 보너스 계산 1곳 |
| `src/bonpuri/components/CardView.tsx` | 수정 — 44·70행 미리보기 2곳 |
| `scripts/verify-bonpuri-contract.ts` | 수정 — 검사 1개 추가 |

**절대 손대지 말 것**: `core/` 나머지, `content/*`, `data/*`, `run/*`, `services/*`, 그 외 `components/*`, `scripts/simulate-bonpuri.ts`(비교 기준), `ref/`, `dadadak/`, `games/`, `vite.config.ts`.

## 3. 규칙

```ts
export const BOND_STACK_CAP = 3;   // core/battle.ts 에서 export

// battle.ts:289
const bonus = played.bond ? played.bond.perStack * Math.min(stacks, BOND_STACK_CAP) : 0;
```

- **`playedMyths` 카운트 자체는 계속 올라간다** (기록·추적용). 상한은 **보너스 배수에만** 적용된다
- `CardView` 의 미리보기 2곳(44·70행)도 같은 상한을 적용한다 — `BOND_STACK_CAP` 을 import 해서 쓰고, 숫자 3을 중복 하드코딩하지 마라
- 카드의 `연계 ... → 이번 +N` 표시가 상한 적용 후 실제 값과 일치해야 한다

## 4. 수용 기준

```
npm run bonpuri:contract
npx tsc --noEmit
npm run build
npm run bonpuri:sim -- --seeds 200 --grid
npm run bonpuri:sim -- --seeds 200 --bonds
```

앞의 셋은 exit 0. **계약 검사는 64개**가 된다 (기존 63 + 신규 1).

### 신규 검사

| # | 검사 |
| --- | --- |
| 64 | 같은 그룹 5장째 카드(스택 4)의 보너스가 `perStack × 3` 이고, 4장째(스택 3)와 동일하다. 3장째(스택 2)는 `perStack × 2` 로 기존과 동일 (검사 19의 0·1·2스택 동작 불변) |

기존 검사 19(연계 누적)는 스택 0·1·2 구간이라 **그대로 통과해야 한다.**

## 5. 완료 보고 (30줄 이내)

1. 변경 파일 목록
2. 수용 커맨드 5개 실행 결과
3. **그리드 전후 비교** — 아래 표를 채워라 (B11 이전 값은 적어뒀다)

| 피해/명 | 문전좌정 B10 | 문전좌정 B12 | 차사처형 B10 | 차사처형 B12 | 기본덱 B10 | 기본덱 B12 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1.0/1.0 | 100% | ? | 100% | ? | 100% | ? |
| 1.3/1.3 | 99.5% | ? | 100% | ? | 65.0% | ? |
| 1.6/1.3 | 83.0% | ? | 98.0% | ? | 2.0% | ? |
| 1.6/1.4 | 82.5% | ? | 93.0% | ? | 0.0% | ? |

4. **`--bonds` 재측정** — 문전좌정의 "총 가치 중 보너스 비중"이 71.8%(1.0/1.0)에서 얼마로 내려갔는지
5. 기대값과 어긋난 항목 (수정하지 말고 그대로 보고)
6. 해석·가정 / 미해결

## 6. 금지사항

- `scripts/simulate-bonpuri.ts` 수정 (비교 기준)
- `playedMyths` 카운트 로직 변경 — 상한은 보너스 계산에서만
- perStack 값·카드 수치 변경
- 결과가 기대와 달라도 상한 값(3)을 임의 조정 — 그대로 보고하라
- 검사 완화·삭제·skip
