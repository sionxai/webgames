# B13 스펙 — 승천 표 v2 (데이터 기반 확정)

근거: B12 후 확장 그리드 실측 (HP 배율 축을 2.4까지 확장, seeds 200, `defensive`+정화). 충돌 시 이 문서가 우선한다.

## 1. 목표

승천 표를 실측 데이터로 재설계해 **확정된 난이도 목표**를 맞춘다.

| 지점 | 목표 | 실측 근거 (그리드 셀) |
| --- | --- | --- |
| 승천 0 · 기본덱 | 70~80% | (피해 1.4 / 명 1.2) → **78.0%** |
| 승천 10 · 최강덱 | 20~30% | (피해 1.6 / 명 2.0) → 문전좌정 **27.5%** (차사 11.5%) |

핵심 발견: **명 배율 1.8~2.0에서 차사처형의 속살 면역이 깨진다** (1.4에서 91.5% → 2.0에서 11.5%). B8 표가 못 잡던 지렛대다.

B8 표의 결함 3건(승천 8 무효·계단 절벽·문전 강제)을 함께 해소한다.

## 2. 변경 파일 (목록 밖 수정 금지)

| 파일 | 성격 |
| --- | --- |
| `src/bonpuri/content/ascension.ts` | 수정 — §3 표로 교체 |
| `scripts/verify-bonpuri-contract.ts` | 수정 — 승천 관련 검사 갱신 |

**절대 손대지 말 것**: `core/*`, `content/cards.ts`, `content/enemies.ts`(원본 적 수치는 계속 동결 — 난이도는 승천 0의 배율로 건다), `data/*`, `run/*`, `services/*`, `components/*`, `scripts/simulate-bonpuri.ts`, `ref/`, `dadadak/`, `games/`, `vite.config.ts`.

## 3. 승천 표 v2 (절대값)

| 승천 | 적 피해 배율 | 적 명 배율 | 시작 명 | 정화 회복 | 보스 개시 액 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 1.40 | 1.20 | 70 | 8 | 0 |
| 1 | 1.45 | 1.25 | 70 | 8 | 0 |
| 2 | 1.45 | 1.35 | 70 | 8 | 0 |
| 3 | 1.50 | 1.40 | 70 | 8 | 0 |
| 4 | 1.50 | 1.50 | 65 | 8 | 0 |
| 5 | 1.55 | 1.60 | 65 | 6 | 0 |
| 6 | 1.55 | 1.70 | 65 | 6 | 0 |
| 7 | 1.60 | 1.80 | 60 | 6 | 0 |
| 8 | 1.60 | 1.85 | 60 | 6 | 0 |
| 9 | 1.60 | 1.95 | 60 | 4 | 0 |
| 10 | 1.60 | 2.00 | 55 | 4 | 0 |

설계 근거:

- **승천 0이 (1.40/1.20)** — 기본 게임 자체가 목표 난이도가 된다. `content/enemies.ts` 는 계속 동결하고 배율로만 조정한다 (측정 연속성 유지)
- **명 배율이 주 진행 축** (1.20 → 2.00, 걸음 ≤0.10) — B9~B12에서 유일하게 전 덱에 먹히는 지렛대로 검증됨. 피해 배율은 1.40→1.60으로 완만하게 (기본덱 절벽이 피해 축에서 발생했으므로 걸음 0.05)
- **보스 개시 액은 전 단계 0** — B8 실측에서 무효로 판명(`defensive` 가 방어량으로 흡수). 필드는 유지하되 죽은 지렛대임을 주석으로 남긴다
- 시작 명·정화 회복은 보조 축 (그리드는 70/8 고정 측정이므로 상위 승천 실제 클리어율은 그리드 셀보다 낮게 나온다 — 의도된 여유)

## 4. 계약 검사 갱신

| # | 갱신 내용 |
| --- | --- |
| 58 | (교체) 승천 0 = 피해 ×1.40 · 명 ×1.20 · 시작 명 70 · 정화 8. **더 이상 "무배율과 동일"이 아니다** — B8의 회귀 앵커였고 B13에서 의도적으로 변경 |
| 59 | (갱신) 승천 5 = attack ×1.55, `block`·`applyStatus` 불변 |
| 61 | (교체) 전 승천에서 보스 개시 액 = 0 (죽은 지렛대 고정 확인) |
| 63 | (유지) `startMiniRun` 승천 인자 생략 = 승천 0 |

나머지 검사(60 원본 불변, 62 결정성 포함)는 그대로 통과해야 한다. **총 64개 유지.**

## 5. 수용 기준

```
npm run bonpuri:contract
npx tsc --noEmit
npm run build
npm run bonpuri:sim -- --seeds 200 --sweep 0-10
```

앞의 셋은 exit 0. 스윕 결과에서:

- **승천 0 기본덱(정화 기준)이 70~80% 구간**에 들어가는가
- **승천 10 최강덱(문전·차사 중 높은 쪽, 정화 기준)이 20~30% 구간**에 들어가는가
- 단계별 하락이 단조롭고(역전 없음), 한 단계 낙폭이 극단적이지 않은가

**목표를 벗어나도 표를 임의 수정하지 마라.** 그대로 보고하면 미세조정은 내가 판단한다 (시작 명·정화 축 여유분 때문에 그리드 예측보다 낮게 나올 수 있음 — 예상된 편차다).

## 6. 완료 보고 (30줄 이내)

1. 변경 파일 목록
2. 수용 커맨드 4개 실행 결과
3. **스윕 표 전문** (승천 0~10 × 기본덱·문전좌정·차사처형, 카드선택/정화)
4. 목표 2지점 판정 한 줄
5. 기대값과 어긋난 항목 (수정하지 말고 그대로 보고)
6. 해석·가정 / 미해결

## 7. 금지사항

- `content/enemies.ts`·`scripts/simulate-bonpuri.ts` 수정
- §3 표 값의 임의 조정
- 검사 완화·삭제·skip (58·59·61은 스펙이 지시한 갱신만)
